export class MysterieBox {
    // defining the properties of the table class with default values
    id: number;
    price: number;
    // constructor of the table class
    constructor(id: number, price: number) {
        this.id=id;
        this.price=price;
     }
  }